# ZITADEL project/application settings
ZITADEL_ISSUER_URL=https://nualogic-test-6uqqo7.eu1.zitadel.cloud/
ZITADEL_CLIENT_ID=383365284251123515
# Add ZITADEL_CLIENT_SECRET only if the ZITADEL app is configured for client authentication.
ZITADEL_REDIRECT_URI=http://localhost:3000/api/auth/callback
ZITADEL_POST_LOGOUT_REDIRECT_URI=http://localhost:3000/
# Set true only after the exact post-logout URI is registered in ZITADEL.
ZITADEL_PROVIDER_LOGOUT=false

# Public browser URL and server-side API destination
APP_URL=http://localhost:3000
API_INTERNAL_URL=http://localhost:8080

# Use a random value of at least 32 characters in real deployments.
SESSION_SECRET=replace-with-a-unique-random-session-secret
# Must match the API process value. Never expose this as NEXT_PUBLIC_*.
VPS_WEB_SHARED_SECRET=replace-with-a-different-random-api-secret
